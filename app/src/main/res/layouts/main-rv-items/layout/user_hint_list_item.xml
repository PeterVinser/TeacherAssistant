<?xml version="1.0" encoding="utf-8"?>
<layout xmlns:tools="http://schemas.android.com/tools"
    xmlns:android="http://schemas.android.com/apk/res/android">

    <data>

        <variable
            name="userHint"
            type="com.piotrokninski.teacherassistant.model.adapteritem.SearchUserAdapterItem.UserAdapterHint" />
    </data>

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="vertical">

        <com.google.android.material.card.MaterialCardView
            android:id="@+id/user_hint_item_card_view"
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:layout_margin="4dp">

            <TextView
                android:id="@+id/user_hint_full_name"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:padding="8dp"
                android:text="@{userHint.fullName}"
                android:textSize="20sp"
                tools:text="Imię i nazwisko" />

        </com.google.android.material.card.MaterialCardView>
    </LinearLayout>

</layout>